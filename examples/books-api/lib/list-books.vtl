#set($inputRoot = $input.path('$'))
[
  #foreach($book in $inputRoot.Items){
      "isbn": "$book.item.S",
      "name": "$book.name.S",
      "authors": [
      #foreach($author in $book.authors.L)"$author.S"#if($foreach.hasNext),#end
      #end
      ]
  }#if($foreach.hasNext),#end
  #end
]
