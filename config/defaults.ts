/**
 * Centralized default configuration for all projects.
 * Change settings here once, and they apply everywhere.
 */

/**
 * Minimum Node.js version.
 */
export const NODE_MIN_VERSION = '20.19.0';

/**
 * Projen version. Manually update to avoid breaking changes.
 */
export const PROJEN_VERSION = '0.98.22';

/**
 * PNPM version to use.
 */
export const PNPM_VERSION = '10.23.0';

/**
 * Command to run projen with pnpm.
 */
export const PROJEN_COMMAND = 'pnpm exec projen';
